<!DOCTYPE html>
<html lang="nb">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logg inn - WCAG Vurderinger</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>WCAG Tilgjengelighetsvurderinger</h1>
    </header>

    <main>
        <div class="auth-container">
            <h2>Logg inn</h2>
            
            <% if (error) { %>
                <div class="error-message" role="alert">
                    <p><%= error %></p>
                </div>
            <% } %>
            
            <form action="/login" method="POST" class="auth-form">
                <div class="form-group">
                    <label for="username">Brukernavn</label>
                    <input type="text" id="username" name="username" required minlength="3">
                </div>
                
                <div class="form-group">
                    <label for="password">Passord</label>
                    <input type="password" id="password" name="password" required minlength="6">
                </div>
                
                <button type="submit" class="btn-primary">Logg inn</button>
            </form>
            
            <p class="auth-link">Har du ikke en konto? <a href="/register">Registrer deg</a></p>
            <p><a href="/">Tilbake til forsiden</a></p>
        </div>
    </main>
</body>
</html>

